Q.10)How can we club two or more rows or columns into a single row or column in an HTML table

A) if we want to club two or more rows or columns into a single row we use rowspan and to combine two or more rows into a single columns we use columnspan