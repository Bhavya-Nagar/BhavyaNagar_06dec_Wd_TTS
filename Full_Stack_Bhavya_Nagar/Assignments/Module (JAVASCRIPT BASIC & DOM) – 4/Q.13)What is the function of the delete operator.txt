Q.13)What is the function of the delete operator

A) suppose we want to delete the property from the object or even suppose we want to delete and element from the array at that we can use the delete operator.